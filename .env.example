# docker-compose reads variables from a .env file in this same directory
# (repo root) automatically. Copy this to .env and fill in real values.
# Never commit .env itself.

REPLICATE_API_TOKEN=
REPLICATE_MODEL_VERSION=
